[project]
name = "pi-cad-prime-skill"
version = "0.9.0"
description = "Prime-facing Python API for Pi-CAD"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/cad"]
