# Regeneration and delivery

Test nominal and meaningful boundary parameter values. A model is robust only if valid changes regenerate without self-intersection, missing references, or silent topology corruption.

Validate solid count, watertightness, bounds, volumes, and critical dimensions after build. Treat STEP as the authoritative exchange artifact while keeping the generator and parameter source alongside it for traceability.
