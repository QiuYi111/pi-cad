# build123d part asset

Copy this directory into an allowed model-source location, replace named dimensions and stable geometric construction, then run it with the Pi-CAD project uv environment. `PI_CAD_OUTPUT` selects the STEP output.
