# build123d assembly asset

Replace the example components with semantic labels, explicit datum-based Locations/joints, and an assembly/interface record. Exported geometry does not replace those contracts.
