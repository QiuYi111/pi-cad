# UX-01：隔离测试配置，恢复正常启动

**What to build:** 真实安装版使用用户自己的项目；测试使用独立配置，失效目录能重新选择。

**Blocked by:** 无，可立即开始

**Status:** ready-for-agent

- [ ] 显式测试配置路径由主进程设置并验证；测试前后正式配置字节一致，测试目录不进入最近项目。
- [ ] 不存在目录在启动 Agent 前被识别，给出选择项目入口，保留草稿；WSL 不可用与目录缺失分别提示。
- [ ] 用有效项目取得真实 RPC 响应；再通过界面发送请求取得真实回复。get_state 不等于账号调用成功。
- [ ] 附真实操作和视觉证据；未执行项明确标记，不用已有测试通过代替本票验收。
