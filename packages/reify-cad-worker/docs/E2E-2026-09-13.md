# RES-263 real CAD E2E result

- Date: 2026-09-14 01:19 Asia/Shanghai
- Host mode: WSL Codex → local MCP (`packages/reify-cad-worker/mcp.mjs`) → Prime sidecar RPC → Pi-CAD
- Model/provider: `openai-codex/gpt-5.6-sol`, thinking `minimal`
- Wall time: about 89 minutes across three revision attempts
- Worker activity: 12 recorded Prime turns, 96 tool executions, 0 MCP-visible tool errors
- Session: `cad-1bd2e8e0-43b5-4735-ae8e-d4cf7da003bd`
- Prime session: `01a09b75-d720-75d6-8422-b36ba4edd717`

## Commands / flow

1. MCP `initialize`, `tools/list` (10 tools).
2. `cad_worker.start` with `mechanical.design` and a bounded foldable phone stand objective.
3. Worker reported the Prime turn and compact tool activity.
4. Pi-CAD entered grilling and waited for material confirmation; Codex used `cad_worker.send` on the same session to confirm assumptions.
5. Worker continued through concept image, CAD build, managed visual observation, and multiple geometric probes.
6. After two independent-review failures and route invalidation, Codex attempted a bounded final revision, then used `cad_worker.cancel` and `cad_worker.close`.

## Honest outcome

Blocked before delivery. The task produced usable-looking STEP/source/concept artifacts, but final independent review did not PASS and the workflow returned to `grilling`; the artifacts must not be treated as released. This is an honest blocker rather than a false PASS.

## Remaining artifacts (uncommitted workspace evidence)

- `foldable_phone_stand.py`
- `foldable_phone_stand.step`
- `foldable_phone_stand_concept.png`
- Hashes: see `E2E-2026-09-13-sha256.txt` (committed next to this file)
- Raw session log: `.prime-sessions/01a09b75-d2ae-738c-a192-25755df8e3fb.jsonl`

## Integration findings

- Same-session blocker question/resume and repeated follow-ups worked.
- Windows/WSL path adapter was exercised at unit level; WSL host mode was exercised end-to-end. Windows host E2E remains outstanding.
- `mechanical.mechanism-design` is absent from the current workflow catalog, so `mechanical.design` was used.
- A race can occur when internal Prime/reviewer turns resume immediately after `agent_end`; the worker should track Prime runtime state rather than trust only `get_state`, and fall back from `prompt` to `steer` when Prime reports queued input. This fix is included in code/tests after the E2E.
- MCP status synchronization was repaired during the run to pass the canonical project directory to the Agent API; the long-running E2E process predates that live patch, so its MCP status showed `unknown` while compact events remained correct.

# Windows Codex → WSL successful quick-build E2E

- Date: 2026-09-14 01:54 Asia/Shanghai
- Chain: Windows Node MCP wrapper → `wsl.exe` → same Reify MCP server → Prime sidecar RPC → Pi-CAD
- Workflow: `mechanical.quick-build`
- Model/provider: `openai-codex/gpt-5.6-sol`, thinking `minimal`
- Wall time: about 123 seconds from start to `done`
- Activity: 2 Prime turns, 11 tool executions, 0 tool errors
- Session: `cad-132b074c-1e43-471a-9ffe-58bec96be456`
- Status result: `outcome: done`; workflow `mechanical.quick-build@1.0.0`, phase `done`
- Returned artifacts:
  - `C:/Users/Admin/AppData/Local/Temp/res263-win-cad5/mounting_plate.py`
  - `C:/Users/Admin/AppData/Local/Temp/res263-win-cad5/mounting_plate.step`
- Hashes: recorded in the run's local `windows-artifact-sha256.txt`, which is not committed.

The wrapper passed `C:\\...` through `wslpath`, ran the identical WSL server, returned compact status/events, and listed STEP/source handles. This completes the required Windows-host real CAD path; the earlier `mechanical.design` run remains an honest blocked example.
