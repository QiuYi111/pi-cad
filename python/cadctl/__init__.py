"""Pi-CAD deterministic geometry backend.

V0 walking skeleton: build123d/OCP execution only.  This package is
deliberately free of engineering semantics.  It reads, writes, renders,
measures and compares geometry; it never labels a face as a "mounting
face" or decides whether a design is good.
"""

__version__ = "0.6.0"
