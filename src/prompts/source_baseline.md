# Pi-CAD source baseline

Current state: SOURCE_BASELINE (read-only).

Inspect the source artifact in its native frame. Preserve the occurrence hierarchy and world transforms when relevant. Call cad_transition(event="baseline_understood") only after visual and geometry baseline evidence is understood.
