# 27：验证并采用经验候选

**状态：** 已完成（2026-03-10）。

**交付结果：** 管理员查看真实失败回放与保留任务回归，通过后批准一个可回退的规则版本。

**前置依赖：** 26 经验改进仅生成候选。

**验收条件：**

- [x] 管理员从 Experience 界面显式触发 checkpoint replay；baseline 和 candidate 在各自目录运行，每个 case 必须通过真实 `engineeringCheck` 进程。模型文字 PASS 但工程检查失败计为 false pass，不能采用。
- [x] validation report 记录 baseline/candidate 文字通过数、工程通过数、误通过、耗时、费用（不可获得时明确 null）、来源失败和修改文件。批准后先保存含 previous/approved tree 与 manifest 的固定 rule version，再显式切换生产树。
- [x] repair 与 guard 任一工程检查失败即拒绝；验证期间生产 digest 改变也拒绝。固定版本携带 previous tree，rollback 恢复原规则；现有运行仍依赖已固定的 workflow/registry snapshot。
- [x] 经验测试 10/10 和桌面生产构建通过。

**范围限制：** 不承诺无人工介入的自我改进。

**开发方式：** 先核对已有实现与未合并修改；沿上述用户行为打通所需状态、真实执行、界面和测试。必要整理先做且保持原行为；不要另拆为纯前端或纯后端票。若一个独立上下文无法完成，按可演示行为继续拆分。

**演示：** 从用户入口执行“交付结果”，展示成功成果、一个相关失败及恢复；以验收中的实际数据核对结果。

**真实操作证据：** 测试的原版与候选都收到文字 PASS，但原版工程命令退出失败、候选工程命令成功；validation 记录 baselineEngineeringPasses=0、candidateEngineeringPasses=1、falsePasses=0。采用后生产 skill 出现候选修复，随后按 version manifest 回退，生产 skill 恢复到原始字节。
