# 25：选择固定规则版本并回退

**状态：** 已完成（2026-03-10）。

**交付结果：** 管理员采用已批准的规则版本，新任务使用新版，旧任务继续按启动时版本运行。

**前置依赖：** 21 提交候选并处理机器审查。

**验收条件：**

- [x] 工作流发现保留全部已安装版本；同一 ID 有多个版本且管理员未采用时 fail closed，不再自动选择最高版本。采用记录精确绑定 ID/version，任务继续固定完整 workflow snapshot、推荐技能引用和 Registry Contract（执行代码语义、schema 与运行依赖版本）。
- [x] 每个任务的 workflow/registry snapshot 仍存入不可变事务；兼容性只检查该任务使用的 registry 条目，因此不相关工具更新不会阻塞旧任务。全局安全策略版本在 adoption policy 中独立记录，不随工作流回退。
- [x] Workflows 页面区分 adopted/available，可由本机管理员为“新任务”采用版本；原子策略文件记录采用人、时间、from/to 历史，选择旧版本即为范围清楚的回退。
- [x] 桌面生产构建、工作流包测试 5/5 通过。

**范围限制：** 不做未经需要验证的通用配置平台。

**开发方式：** 先核对已有实现与未合并修改；沿上述用户行为打通所需状态、真实执行、界面和测试。必要整理先做且保持原行为；不要另拆为纯前端或纯后端票。若一个独立上下文无法完成，按可演示行为继续拆分。

**演示：** 从用户入口执行“交付结果”，展示成功成果、一个相关失败及恢复；以验收中的实际数据核对结果。

**真实操作证据：** 测试同时安装 `custom.versioned@1.0.0` 与 `@2.0.0`，未采用时启动被拒绝；采用 1.0.0 后首个任务固定其 snapshot，随后另一个新项目采用 2.0.0 并启动新版。再次直接恢复首个 run，其版本和 phase purpose 仍为 1.0.0 内容。
