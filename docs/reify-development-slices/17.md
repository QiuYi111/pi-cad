# 17：在 Blender 编辑后回到 Reify

**状态：** 已完成（2026-03-10，Linux/WSLg 外部编辑路径）。

**交付结果：** 用户从当前成果进入 Blender 完整编辑，保存后返回 Reify 更新同一场景。

**前置依赖：** 16 Blender 场景预览与渲染。

**验收条件：**

- [x] 在 Linux/WSLg 用真实 Blender 原生进程打开同一 `.blend`；Blender 获得独立窗口焦点和完整输入，启动日志确认文件载入。当前 GPU/EGL 初始化失败时 Blender 回退而未破坏文件；关闭由 Blender 自身或进程终止处理。
- [x] 采用同文件外部打开与返回刷新。完整 Blender 不能可靠嵌入 Electron 的跨平台窗口层级；界面明确提示在 Blender 保存，再点“I’m back”刷新。
- [x] 刷新重新读取场景并计算文件 SHA-256，同时清除旧渲染，避免把旧图冒充编辑后预览。未保存修改与退出确认由 Blender 原生处理，Reify 只读取磁盘已保存版本。
- [x] 真实文件编辑前后哈希变化得到验证，桌面生产构建通过，`git diff --check` 通过。

**范围限制：** 不预先承诺跨平台嵌入完整 Blender；其余平台随原生安装验证。

**开发方式：** 先核对已有实现与未合并修改；沿上述用户行为打通所需状态、真实执行、界面和测试。必要整理先做且保持原行为；不要另拆为纯前端或纯后端票。若一个独立上下文无法完成，按可演示行为继续拆分。

**演示：** 从用户入口执行“交付结果”，展示成功成果、一个相关失败及恢复；以验收中的实际数据核对结果。

**真实操作证据：** Blender 5.0.1 原生窗口启动并读取 `/tmp/reify-blender-test/product.blend`；环境记录了 EGL/Zink 初始化失败并由 Blender 继续打开场景。把 Part 的 X scale 保存为 1.5 后，文件 SHA-256 从 `ff5aa31e…` 变为 `a0df43be…`，刷新仍读取 Hero 活动相机、5 objects 和 1–250 帧。Reify 的外部编辑流程显示保存/返回说明，刷新后以新哈希绑定后续渲染。
