# 16：Blender 场景预览与渲染

**状态：** 已完成（2026-03-10）。

**交付结果：** 用户打开或由 Agent 生成 Blender 场景，在画布查看预览、相机和真实渲染结果。

**前置依赖：** 12 工具结果直接进入成果列表；05 停止任务并继续工作。

**验收条件：**

- [x] `.blend` 作为带路径、角色和 SHA-256 的一等成果进入目录；缺少 Blender 时可从场景画布按需安装固定哈希的 4.5.3 Linux 运行时，渲染进程可停止。
- [x] 画布通过 Blender 后台真实读取场景、对象数、帧范围、相机和活动相机；相机可切换并生成 PNG。失败只更新错误提示，上一张成功渲染继续保留。
- [x] 用本机 Blender 5.0.1 创建并打开真实 `.blend`，读取 `Camera`/`Hero` 两台相机，选择 `Hero` 渲染出 160×120 RGBA PNG；结果立即加入同一个 Artifacts/Results 列表。缺失相机产生明确失败，不使用静态参考图作为预览。
- [x] 桌面生产构建通过，viewer 成果映射测试 8/8 通过，`git diff --check` 通过。

**范围限制：** 完整 Blender 原生编辑界面接入由下一票验证。

**开发方式：** 先核对已有实现与未合并修改；沿上述用户行为打通所需状态、真实执行、界面和测试。必要整理先做且保持原行为；不要另拆为纯前端或纯后端票。若一个独立上下文无法完成，按可演示行为继续拆分。

**演示：** 从用户入口执行“交付结果”，展示成功成果、一个相关失败及恢复；以验收中的实际数据核对结果。

**真实操作证据：** `/tmp/reify-blender-test/product.blend` 由 Blender 实际生成；`desktop-blender-scene.py inspect` 返回 5 objects、活动相机 Hero 与 1–250 帧，`render --camera Hero` 写出经 `file` 验证的 160×120 PNG。`--camera Missing` 返回 `Camera not found: Missing`，随后有效相机仍可渲染。应用构建与 8 个 viewer 测试通过。
