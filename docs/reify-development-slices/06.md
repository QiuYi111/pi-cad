# 06：运行中追加或调整需求

**状态：** 已完成。

**当前进展（2026-09-06）：**

- 运行中输入默认进入当前项目的持久队列；每项带 UUID，可查看、编辑、取消，按入队顺序执行。
- 输入区可选择任务完成后执行、停止并修改或只记备注；停止并修改等待停止确认后才进入执行队列。
- 队列按项目路径隔离并写入本地持久状态；失败后移除已尝试请求并继续处理下一项，避免重复提交。
- 桌面 E2E 连续执行两条修改，覆盖编辑、取消、备注、停止并修改以及最终继续构建；完整产品路径通过。

**交付结果：** 用户可选择任务完成后执行、停止并修改，或仅记为备注。

**前置依赖：** 05 停止任务并继续工作。

**验收条件：**

- [x] 默认排队，明确执行时机；待办消息可查看、编辑、取消，记录请求身份防止重复执行。
- [x] 停止并修改复用停止流程；只在旧任务停止后启动新需求；备注不自动执行。
- [x] 任务失败、重启和项目切换后队列归属正确；以连续两条修改验证次序和最终模型。
- [x] 遵守总计划的共同完成标准，提交真实操作证据与相关失败验证。

**范围限制：** 首版不支持在任意底层工具步骤中热修改。

**开发方式：** 先核对已有实现与未合并修改；沿上述用户行为打通所需状态、真实执行、界面和测试。必要整理先做且保持原行为；不要另拆为纯前端或纯后端票。若一个独立上下文无法完成，按可演示行为继续拆分。

**演示：** 从用户入口执行“交付结果”，展示成功成果、一个相关失败及恢复；以验收中的实际数据核对结果。

