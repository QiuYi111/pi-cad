# 02：一句需求得到首个 CAD

**状态：** 已完成。

**当前进展（2026-09-06）：**

- 已新增 `mechanical.quick-build`：明确单零件直接进入建模，不要求概念图或装配阶段，可直接交付或选择机器审查。
- 已新增 `cad.save_and_check(...)`，严格复用原 `cad.commit()` 和 `cad.model.build()`；提交授权失败时不启动构建。
- Python 包测试 29 项、核心 TypeScript 测试 294 项通过。
- 已用真实 Prime + sidecar + 沙箱完成固定带孔支架：80×40×8 mm，中心通孔 Ø12 mm，单实体；流程进入 `done`。
- 已修复无发布义务的轻量流程被完成门误判为缺少 release，并补回归测试。
- 桌面事件归类已识别 `save_and_check`，能显示真实构建成果来源；重复审查仍由既有候选幂等约束覆盖。

**交付结果：** 用户在完整对话中提出明确的单零件需求，真实 Agent 构建模型并放入画布。

**前置依赖：** 01 打开 STEP，进入大画布。

**验收条件：**

- [x] 接通真实 Prime、sidecar、Python 和 Viewer，保留流式文字、工具结果及当前成果来源。
- [x] 实现 quick-build 与 save_and_check 的最小完整路径；按流程选择检查和视图，不强制概念图；宏仍经过原授权与事务。
- [x] 固定带孔支架任务验证尺寸与孔径；宏和手动组合结果一致，重复请求不重复提交。
- [x] 遵守总计划的共同完成标准，提交真实操作证据与相关失败验证。

**范围限制：** 不包含装配、正式批准；可使用已有开发环境及登录状态。

**开发方式：** 先核对已有实现与未合并修改；沿上述用户行为打通所需状态、真实执行、界面和测试。必要整理先做且保持原行为；不要另拆为纯前端或纯后端票。若一个独立上下文无法完成，按可演示行为继续拆分。

**演示：** 从用户入口执行“交付结果”，展示成功成果、一个相关失败及恢复；以验收中的实际数据核对结果。
