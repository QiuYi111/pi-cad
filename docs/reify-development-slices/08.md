# 08：选择已有参数并预览修改

**状态：** 已完成。

**当前进展（2026-09-06）：**

- 参数面板显示源文件、源哈希、单位、合法范围、已应用值和当前预览值；无 manifest 的 CAD 显示带 artifact 路径的 Agent 修改入口。
- 预览以代次隔离迟到结果；Reset preview 回到已应用值并清除临时网格，不修改模型；应用通过授权 parameter-edit 工作流并刷新成果身份。
- Viewer 明确区分显示外观与分析工作流中的工程材料属性。
- 真实集成测试把孔径从 6 mm 改到 12 mm，B-Rep 圆柱半径从 3 mm 变为 6 mm；修改导入依赖后 warm worker 输出深度从 24 mm 变为 30 mm；应用后 Viewer 哈希与 manifest 一致。桌面 E2E 覆盖预览、重置、应用和无绑定退路。

**交付结果：** 用户选中模型中有参数绑定的部位，调整数值、预览并应用。

**前置依赖：** 02 一句需求得到首个 CAD。

**验收条件：**

- [x] 显示参数来源、单位、合法范围、已应用值与预览值；没有绑定时提供引用该部位交给 Agent 修改。
- [x] 预览与应用区分，旧预览不能覆盖新请求；应用后成果身份、哈希与视图一致；材料外观与工程材料区分。
- [x] 用孔径修改核验几何变化；修改导入依赖文件后缓存失效；重置预览不修改已应用模型。
- [x] 遵守总计划的共同完成标准，提交真实操作证据与相关失败验证。

**范围限制：** 不承诺任意 STEP 面都能恢复原建模参数。

**开发方式：** 先核对已有实现与未合并修改；沿上述用户行为打通所需状态、真实执行、界面和测试。必要整理先做且保持原行为；不要另拆为纯前端或纯后端票。若一个独立上下文无法完成，按可演示行为继续拆分。

**演示：** 从用户入口执行“交付结果”，展示成功成果、一个相关失败及恢复；以验收中的实际数据核对结果。

