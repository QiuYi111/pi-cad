# 31：Linux deb 安装并完成首个任务

**状态：** 包与恢复实现完成；干净 Ubuntu 安装验收待执行。

**交付结果：** 用户在声明支持的 Ubuntu 环境安装 deb，从桌面入口启动并完成建模导出。

**前置依赖：** 28 首次启动、登录与创建项目；02 一句需求得到首个 CAD。

**验收条件：**

- [x] 生成 `Reify-Linux-amd64.deb`，自动检查 Debian metadata、Reify.desktop、品牌图标和 bundled runtime；安装说明声明 Ubuntu 22.04/24.04 x64 与用户数据位置。
- [x] 基础 runtime 与可选 ParaView/Blender/仿真分开，Settings 保留依赖错误和重试入口。
- [x] 当前 WSL Ubuntu 已完成真实打包、包内容检查；`linux-unpacked` 已验证 native runtime、启动、真实 STEP 解析、坏文件恢复和导出副本。
- [ ] 干净桌面 Ubuntu 的 deb 安装、真实任务和卸载保留项目仍待人工验收。

**范围限制：** 其他发行版不默认宣称支持。

**开发方式：** 先核对已有实现与未合并修改；沿上述用户行为打通所需状态、真实执行、界面和测试。必要整理先做且保持原行为；不要另拆为纯前端或纯后端票。若一个独立上下文无法完成，按可演示行为继续拆分。

**演示：** 从用户入口执行“交付结果”，展示成功成果、一个相关失败及恢复；以验收中的实际数据核对结果。
