# 29：Windows 复用已有 WSL 完成首个任务

**状态：** 实现完成；安装后真实 Agent 全链路待人工验收。

**交付结果：** 用户通过 Windows 安装包启动，选择或复用受支持 WSL 环境，完成一个 CAD 任务。

**前置依赖：** 28 首次启动、登录与创建项目；02 一句需求得到首个 CAD。

**验收条件：**

- [x] 首启分步显示环境进度、发行版和项目位置；WSL bridge 使用 argv 与 stdin pipe，Windows/UNC 路径转换有测试覆盖。
- [x] ParaView、Blender、仿真均排除在基础 CAD readiness 之外；runtime manifest 相同即复用，重复启动不安装。
- [x] 当前源码生成的真实 NSIS 已在 Windows 主机验证安装、启动、快捷方式和卸载项；实际安装路径完成 bundled runtime、已有项目恢复、真实参数化 STEP 预览与宽度修改，页面切换后仍保持应用值。
- [ ] 真实个人 OAuth 与签名发行包仍需外部账号和证书；当前 E2E 身份替身不能作为该项证据。

**范围限制：** 不包含从零启用 WSL，见下一票。

**开发方式：** 先核对已有实现与未合并修改；沿上述用户行为打通所需状态、真实执行、界面和测试。必要整理先做且保持原行为；不要另拆为纯前端或纯后端票。若一个独立上下文无法完成，按可演示行为继续拆分。

**演示：** 从用户入口执行“交付结果”，展示成功成果、一个相关失败及恢复；以验收中的实际数据核对结果。
