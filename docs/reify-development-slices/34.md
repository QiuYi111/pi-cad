# 34：Windows 更新与回退保留项目

**状态：** 手动更新实现完成；双版本安装验收待签名包。

**交付结果：** 用户安装新版后继续旧项目；更新失败时可回到可用版本。

**前置依赖：** 29 Windows 复用已有 WSL 完成首个任务；07 项目与对话切换后恢复。

**验收条件：**

- [x] Settings 显示版本、平台、架构、NSIS 渠道、项目与用户数据位置；明确停止活动任务后再手动更新。
- [x] NSIS 配置 `deleteAppDataOnUninstall: false`，项目在用户选择目录；说明要求按 release manifest 的 schema 兼容性决定回退，数据删除独立执行。
- [x] 当前源码的开发 NSIS 完成安装、启动、快捷方式、卸载项和隔离 user-data profile 检查；安装后已有项目及参数修改路径通过。
- [ ] 缺少两个已签名版本，尚不能完成实际升级失败与回退验收。

**范围限制：** 首版允许受控手动更新，不强制建设自动更新服务。

**开发方式：** 先核对已有实现与未合并修改；沿上述用户行为打通所需状态、真实执行、界面和测试。必要整理先做且保持原行为；不要另拆为纯前端或纯后端票。若一个独立上下文无法完成，按可演示行为继续拆分。

**演示：** 从用户入口执行“交付结果”，展示成功成果、一个相关失败及恢复；以验收中的实际数据核对结果。
