# 28：首次启动、登录与创建项目

**状态：** 实现完成；真实 OAuth 人工验收待执行。

**交付结果：** 用户打开应用，完成环境状态查看、登录和项目创建，进入可继续的工作区。

**前置依赖：** 无，可立即开始。

**验收条件：**

- [x] 暖白品牌首启改为清晰中文，只包含工程环境、ChatGPT、项目三步；支持新建和打开项目，设置与项目路径持久化，可从中断步骤恢复。
- [x] OAuth 支持等待、手动回调、取消、过期与错误重试；凭据只位于用户级 Prime auth 目录。退出登录只删除 auth.json，并明确项目被保留。
- [x] 基础 CAD readiness 排除 ParaView 等可选组件；`onboardingComplete` 持久化后不重复欢迎流程。
- [ ] 真实个人 OAuth 必须由人在浏览器完成；自动测试不会伪称完成该外部账号验收。桌面构建与 auth/runtime 自动测试 17 项通过。

**范围限制：** 不要求完成所有安装平台后才交付本票。

**开发方式：** 先核对已有实现与未合并修改；沿上述用户行为打通所需状态、真实执行、界面和测试。必要整理先做且保持原行为；不要另拆为纯前端或纯后端票。若一个独立上下文无法完成，按可演示行为继续拆分。

**演示：** 从用户入口执行“交付结果”，展示成功成果、一个相关失败及恢复；以验收中的实际数据核对结果。
