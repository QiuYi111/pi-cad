# 11：快速测量与剖切

**状态：** 已完成（2026-03-10）。

**交付结果：** 用户打开 STEP 测量尺寸、查看剖面，也可向 Agent 提出只读检查。

**前置依赖：** 01 打开 STEP，进入大画布。

**验收条件：**

- [x] 接通真实测量回调和剖切，显示单位与对象；结果可引用到对话。
- [x] quick-check 不经过概念、发布阶段，不改源文件；明确当前模型与指定旧成果的检查目标。
- [x] 用已知尺寸模型验证距离和剖面，检查前后文件哈希一致。
- [x] 遵守总计划的共同完成标准，提交真实操作证据与相关失败验证。

**范围限制：** 不增加完整草图或直接建模工具。

**开发方式：** 先核对已有实现与未合并修改；沿上述用户行为打通所需状态、真实执行、界面和测试。必要整理先做且保持原行为；不要另拆为纯前端或纯后端票。若一个独立上下文无法完成，按可演示行为继续拆分。

**演示：** 从用户入口执行“交付结果”，展示成功成果、一个相关失败及恢复；以验收中的实际数据核对结果。

**实现与证据：**

- Viewer 的 Inspect 浮层对当前或历史 STEP 调用真实 `cadctl inspect` / `scan-sections`，显示 X/Y/Z 包围盒、实体数、剖切位置、面积、面数及毫米单位，并把成果路径和 SHA-256 一并带入对话。
- 新增 `mechanical.quick-check` 两阶段只读流程，仅允许读取、观察及写入 run observation；编译测试确认没有 concept、build、review、release 阶段或 `cad_build_step` 权限。
- 已知 `40 × 30 × 12 mm` STEP 的集成测试得到 Z 中剖面 `1200 mm²`，检查前后 SHA-256 均为 `4b92507e2fbd…`。
- 桌面完整产品路径实测 Measure X、Section Z 与引用对话；失败候选仍由同一路径中的过大圆角触发，恢复应用值后 Viewer 保留已应用模型。
