# 03：对话与画布平滑切换

**状态：** 已完成。

**当前进展（2026-09-06）：**

- 已完成完整对话、最近消息、仅输入三档；固定入口和 `Ctrl/⌘ + \\` 循环切换。
- 首个导入或构建成果自动进入画布，画布底部保留悬浮输入。
- 草稿、阅读历史和选中文字时不抢走界面，改为显示“新模型已就绪”；草稿、滚动和 Viewer 状态因组件不卸载而保留。
- 首次进入画布使用短动效，系统减少动画设置会关闭动效；状态栏始终可见。

**交付结果：** 首个可看成果就绪后对话收起，用户随时展开，再返回同一视角。

**前置依赖：** 02 一句需求得到首个 CAD。

**验收条件：**

- [x] 提供仅输入、最近消息、完整对话三档；切换入口位置固定，快捷键按平台显示且不与输入操作冲突。
- [x] 首次切换短动效；输入中、选中文本或主动阅读历史时只提示，不夺取焦点；减少动画模式直接切换。
- [x] 切换后草稿、滚动、视角、选择不丢；后续成果不反复触发整屏动画，关闭侧栏仍能看到任务状态。
- [x] 遵守总计划的共同完成标准，提交真实操作证据与相关失败验证。

**范围限制：** 不建设全局自由窗口管理。

**开发方式：** 先核对已有实现与未合并修改；沿上述用户行为打通所需状态、真实执行、界面和测试。必要整理先做且保持原行为；不要另拆为纯前端或纯后端票。若一个独立上下文无法完成，按可演示行为继续拆分。

**演示：** 从用户入口执行“交付结果”，展示成功成果、一个相关失败及恢复；以验收中的实际数据核对结果。
