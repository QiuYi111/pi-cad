# 19：从 Git 恢复并核验重建

**状态：** 已完成（2026-03-10）。

**交付结果：** 用户选择源码版本，在保留当前修改的前提下重建，并看到是否复现原成果。

**前置依赖：** 18 查看与比较设计版本。

**验收条件：**

- [x] 从命名版本执行重建时，使用该版本记录的 Git revision 创建 detached 临时 worktree；参数 manifest 在提交时按内容哈希保存，源码、同 revision 导入依赖、参数和 Python/Git/平台版本都写入结果。
- [x] 新 STEP 写入项目 `.pi-cad/rebuilds/` 作为可继续修改的候选；分别报告字节 SHA-256 一致、几何包围盒/实体数一致、差异或无法验证，不把几何相似声称为字节复现。
- [x] 隔离进程以 `env -C <worktree>` 运行，避免常驻 Python worker 的当前依赖缓存污染历史重建。暂存、未提交和未跟踪文件在操作前后逐字一致；缺 source revision、参数 manifest、输出绑定或源码文件均给出具体失败原因。
- [x] 真实参数化 CAD 集成测试、桌面生产构建、全库 TypeScript 296/296 和 `git diff --check` 通过。

**范围限制：** 不自动解决 Git 冲突，不把 STEP 放入 Git。

**开发方式：** 先核对已有实现与未合并修改；沿上述用户行为打通所需状态、真实执行、界面和测试。必要整理先做且保持原行为；不要另拆为纯前端或纯后端票。若一个独立上下文无法完成，按可演示行为继续拆分。

**演示：** 从用户入口执行“交付结果”，展示成功成果、一个相关失败及恢复；以验收中的实际数据核对结果。

**真实操作证据：** 集成测试创建 40×24×12 mm 带 6 mm 孔的参数化模型，提交源码 revision、STEP 和参数 snapshot；随后加入 staged 与 untracked 用户文件，从 detached worktree 重建，确认几何匹配、参数为 width 40/hole 6、source revision 精确一致，且 `git status --porcelain` 前后完全相同。测试曾真实捕获常驻 worker 把当前 30 mm 依赖带入历史构建的失败，修复后改用隔离 cwd 的独立 cadctl 进程并通过。
