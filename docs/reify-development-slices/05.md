# 05：停止任务并继续工作

**状态：** 已完成。

**当前进展（2026-09-06）：**

- 对话活动明确显示等待、思考、响应、工具执行、失败、完成和停止，并只显示真实耗时。
- 停止按钮在确认期间显示 Stopping…；确认失败会明确提示任务可能仍在停止，成功后旧运行代次不再产出事件。
- Linux 执行器以独立进程组启动任务，先发送 SIGTERM、250ms 后升级 SIGKILL；超时与用户停止均覆盖派生子进程。
- 桌面 E2E 验证长任务停止后没有生成模型且下一任务可完成；TypeScript 全套 295 项通过，包含进程组超时/取消测试。

**交付结果：** 用户看到真实执行状态，停止耗时构建后可立即准备下一条需求。

**前置依赖：** 02 一句需求得到首个 CAD。

**验收条件：**

- [x] 分别显示思考中、工具执行、等待回答、失败、完成；无可靠分母时显示步骤与耗时，不编造百分比。
- [x] 停止沿真实进程链结束 Python、子进程和计算任务；确认停止后拒收旧输出，超时明确显示仍在停止。
- [x] 用长计算和派生子进程验证停止及超时，无后台继续写文件；最后可用成果保留，下一任务可完成。
- [x] 遵守总计划的共同完成标准，提交真实操作证据与相关失败验证。

**范围限制：** 不以隐藏状态卡代替取消执行。

**开发方式：** 先核对已有实现与未合并修改；沿上述用户行为打通所需状态、真实执行、界面和测试。必要整理先做且保持原行为；不要另拆为纯前端或纯后端票。若一个独立上下文无法完成，按可演示行为继续拆分。

**演示：** 从用户入口执行“交付结果”，展示成功成果、一个相关失败及恢复；以验收中的实际数据核对结果。

