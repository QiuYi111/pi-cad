# 26：经验改进仅生成候选

**状态：** 已完成（2026-03-10）。

**交付结果：** 失败任务形成可追踪的技能改进候选，管理员能查看差异与来源。

**前置依赖：** 25 选择固定规则版本并回退。

**验收条件：**

- [x] 经验归档和 immutable cutoff 保留；distiller 只写独立 `distill-jobs/*.candidate` 目录，并在 job report 记录来源失败 seq、逐文件变更范围、candidate root 和 pending validation。
- [x] 删除了 distillation worker 直接删除并替换生产 `skills/`、`workflow-packages/` 的路径。界面显示来源、修改文件和待验证状态，文案明确生产规则未变化。
- [x] 生成期间对生产规则树做前后 digest 并发检查；外部修改会使候选失败。候选进程、schema/replay 描述或编译失败只留下隔离诊断，不污染正式规则。
- [x] 经验测试 9/9 通过，包含真实候选文件生成和生产 skill 字节级不变验证。

**范围限制：** 本票不自动批准或采用候选。

**开发方式：** 先核对已有实现与未合并修改；沿上述用户行为打通所需状态、真实执行、界面和测试。必要整理先做且保持原行为；不要另拆为纯前端或纯后端票。若一个独立上下文无法完成，按可演示行为继续拆分。

**演示：** 从用户入口执行“交付结果”，展示成功成果、一个相关失败及恢复；以验收中的实际数据核对结果。

**真实操作证据：** 测试从低评分失败轨迹生成对 `skills/cad/SKILL.md` 的候选修改，job 状态为 `candidate`、validation 为 `pending`，并记录 failure seq 1 和 changed file。测试随后分别读取 candidate 与生产文件：候选包含修复，生产文件与运行前完全一致。
