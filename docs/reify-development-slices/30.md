# 30：Windows 干净环境安装并继续

**状态：** 实现完成；干净 Windows VM 验收待执行。

**交付结果：** 没有 WSL 的用户完成必要准备，重启后从中断步骤继续首个任务。

**前置依赖：** 29 Windows 复用已有 WSL 完成首个任务。

**验收条件：**

- [x] 首启说明 WSL 是隔离工程环境；区分需重启、需首次初始化 Ubuntu、重试三种动作。项目、发行版与完成状态在重启前持久化，返回后重新探测。
- [x] WSL 安装 heartbeat、runtime 安装进度、OAuth 等待/取消分别显示；错误保留现有环境并提供同一步重试。
- [x] 自动测试覆盖中断分类和已有部分环境的幂等逻辑。
- [ ] 尚未在一台无 WSL 的干净 Windows VM 完成到首次导出。
- [ ] 遵守总计划的共同完成标准，提交真实操作证据与相关失败验证。

**范围限制：** 不隐藏系统权限要求，不假定所有企业机器允许启用 WSL。

**开发方式：** 先核对已有实现与未合并修改；沿上述用户行为打通所需状态、真实执行、界面和测试。必要整理先做且保持原行为；不要另拆为纯前端或纯后端票。若一个独立上下文无法完成，按可演示行为继续拆分。

**演示：** 从用户入口执行“交付结果”，展示成功成果、一个相关失败及恢复；以验收中的实际数据核对结果。
