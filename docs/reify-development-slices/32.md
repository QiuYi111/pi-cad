# 32：Linux AppImage 启动并完成首个任务

**状态：** 包与启动实现完成；干净 Ubuntu UX 验收待执行。

**交付结果：** 用户下载 AppImage，按清晰提示启动、建立桌面入口并完成建模导出。

**前置依赖：** 28 首次启动、登录与创建项目；02 一句需求得到首个 CAD。

**验收条件：**

- [x] 生成并真实启动 `Reify-Linux-x86_64.AppImage`；说明目标 Ubuntu/x64、执行权限、FUSE extract-and-run、应用文件与用户数据/项目分离。
- [x] 桌面集成保持用户选择；Settings 按 appimage channel 显示专用更新方式，不与 deb 混淆。
- [x] 自动检查 AppRun、desktop 文件、runtime payload 和 SHA；打包后的 Linux executable 已完成真实 STEP 解析、坏文件恢复和导出副本。
- [ ] 移动/删除 AppImage 后的干净桌面真实任务仍待人工验收。

**范围限制：** 不依赖 deb 票完成，不承诺任意 Linux 环境即开即用。

**开发方式：** 先核对已有实现与未合并修改；沿上述用户行为打通所需状态、真实执行、界面和测试。必要整理先做且保持原行为；不要另拆为纯前端或纯后端票。若一个独立上下文无法完成，按可演示行为继续拆分。

**演示：** 从用户入口执行“交付结果”，展示成功成果、一个相关失败及恢复；以验收中的实际数据核对结果。
