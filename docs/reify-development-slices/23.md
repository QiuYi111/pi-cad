# 23：生成可核验的正式交付包

**状态：** 已完成（2026-03-10）。

**交付结果：** 用户正式发布到本地目录，得到模型、必要图纸或报告、验收摘要和批准记录。

**前置依赖：** 22 人工批准、撤销与过期；19 从 Git 恢复并核验重建。

**验收条件：**

- [x] Formal package 只接受当前有效的人类批准，绑定 project、commit、workflow hash、Git source revision、approval、scope 和逐文件 role/SHA。复制前、复制后及原子提交前再次核验批准和原文件。
- [x] 所有模型及实际存在的图纸/报告成果、验收摘要、批准记录和 release manifest 先写入目标目录内的 staging；最后一次 `mv` 才产生成功目录。相同版本和批准产生稳定 releaseId，重试验证并复用已有包。
- [x] 并发撤销在最终提交前终止；源产物替换、目标不存在/不可写、复制哈希不符均失败且提示保留的 staging 恢复位置。测试在独立目标目录重新读取 manifest 和模型并核对 SHA。
- [x] 桌面生产构建、正式发布/批准测试 3/3、`git diff --check` 通过。

**范围限制：** 普通导出不叫正式批准发布；没有图纸需求时不凭空生成。

**开发方式：** 先核对已有实现与未合并修改；沿上述用户行为打通所需状态、真实执行、界面和测试。必要整理先做且保持原行为；不要另拆为纯前端或纯后端票。若一个独立上下文无法完成，按可演示行为继续拆分。

**演示：** 从用户入口执行“交付结果”，展示成功成果、一个相关失败及恢复；以验收中的实际数据核对结果。

**真实操作证据：** 测试把 `trusted model` 正式发布到另一个临时目录，读取 `release-manifest.json` 核对 source revision、approvalId，并独立计算 `files/part.step` SHA；第二次发布返回 reused。随后替换源文件，发布被 `changed before packaging` 拒绝；模拟第二次批准检查时撤销，发布在原子 rename 前被 `revoked while the package was being prepared` 拒绝。
