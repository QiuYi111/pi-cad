# 35：Linux 更新与回退保留项目

**状态：** 手动更新实现完成；双版本干净系统验收待执行。

**交付结果：** deb 和 AppImage 用户按各自安装方式升级，继续原项目并能恢复失败更新。

**前置依赖：** 31 Linux deb 安装并完成首个任务；32 Linux AppImage 启动并完成首个任务；07 项目与对话切换后恢复。

**验收条件：**

- [x] 运行时识别 deb/AppImage，Settings 分别给出对应更新步骤和数据位置；文档明确禁止自动跨渠道迁移。
- [x] 两种渠道都要求停止活动任务；项目与 user data 独立于应用，回退受 release schema 兼容声明约束。
- [x] 最新 deb/AppImage 均完成构建、内容与 checksum 检查。
- [ ] 两个版本在干净 Ubuntu 上的升级、失败恢复和回退待人工执行。
- [ ] 遵守总计划的共同完成标准，提交真实操作证据与相关失败验证。

**范围限制：** 不承诺跨安装渠道自动迁移。

**开发方式：** 先核对已有实现与未合并修改；沿上述用户行为打通所需状态、真实执行、界面和测试。必要整理先做且保持原行为；不要另拆为纯前端或纯后端票。若一个独立上下文无法完成，按可演示行为继续拆分。

**演示：** 从用户入口执行“交付结果”，展示成功成果、一个相关失败及恢复；以验收中的实际数据核对结果。
