# 18：查看与比较设计版本

**状态：** 已完成（2026-03-10）。

**交付结果：** 用户选择两个已保留成果版本，比较模型与参数变化，再回到当前版本。

**前置依赖：** 08 选择已有参数并预览修改；12 工具结果直接进入成果列表。

**验收条件：**

- [x] 当前、HEAD、命名提交和仿真版本按状态分组；内部历史默认折叠。Compare 提供并排视图与同步重置视角，退出后回到此前选中的当前版本。
- [x] 每个 CAD 视图携带预期产物 SHA-256；加载同路径文件后哈希不一致会保留原视图并提示重建该版本，绝不把当前文件当作历史成果。
- [x] 比较栏显示 A/B 产物哈希。提交中保留的参数 manifest 会进入目录并显示已知参数差异；没有可验证参数快照时明确写为视觉推测。两种同路径、不同孔径版本由不同哈希保持为两个来源。
- [x] 桌面生产构建通过，viewer 测试 9/9、全库 TypeScript 回归 295/295、`git diff --check` 通过。

**范围限制：** 不建设分布式或永久不可变 STEP 仓库；缺失重建由下一票交付。

**开发方式：** 先核对已有实现与未合并修改；沿上述用户行为打通所需状态、真实执行、界面和测试。必要整理先做且保持原行为；不要另拆为纯前端或纯后端票。若一个独立上下文无法完成，按可演示行为继续拆分。

**演示：** 从用户入口执行“交付结果”，展示成功成果、一个相关失败及恢复；以验收中的实际数据核对结果。

**真实操作证据：** `viewer.test.ts` 构造 Hole 8 mm 与 Hole 10 mm 两个提交，二者使用相同 `build/plate.step` 路径但分别保留 `sha-8`/`sha-10`，成果目录保持两个独立版本。`CadViewer` 在真实加载后核对预期 SHA，错配错误包含 expected/found 和 rebuild 指引。生产构建、9 个 viewer 测试和 295 个全库 TS 测试通过。
