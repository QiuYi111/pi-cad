# 33：macOS 安装并完成首个任务

**状态：** 发布路径实现完成；真实 macOS 签名、公证与任务验收受外部环境阻塞。

**交付结果：** 用户从 DMG 安装并启动 Reify，完成环境准备、登录、建模和导出。

**前置依赖：** 28 首次启动、登录与创建项目；02 一句需求得到首个 CAD。

**验收条件：**

- [x] 品牌 DMG、ICNS、Apple silicon CI、Developer ID 与 notarization 环境已接入；缺任一凭据会明确阻塞 package:mac。说明声明 macOS 14+ Apple silicon。
- [x] native bridge 使用 argv/stdin；作者/审查者 authority 与 subject-scoped reviewer sandbox 由现有安全测试验证。
- [ ] 当前没有 macOS 主机和 Apple 发布凭据，不能真实生成已签名公证包或宣称平台通过。
- [ ] 遵守总计划的共同完成标准，提交真实操作证据与相关失败验证。

**范围限制：** 未在实际平台验证不得宣称支持；不以关闭系统校验为安装方案。

**开发方式：** 先核对已有实现与未合并修改；沿上述用户行为打通所需状态、真实执行、界面和测试。必要整理先做且保持原行为；不要另拆为纯前端或纯后端票。若一个独立上下文无法完成，按可演示行为继续拆分。

**演示：** 从用户入口执行“交付结果”，展示成功成果、一个相关失败及恢复；以验收中的实际数据核对结果。
