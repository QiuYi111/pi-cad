# 20：随版本查看工程验收摘要

**状态：** 已完成（2026-03-10）。

**交付结果：** 用户看到该版本的关键要求、实际检查结果及未验证项，并能打开证据。

**前置依赖：** 12 工具结果直接进入成果列表；18 查看与比较设计版本。

**验收条件：**

- [x] 命名版本内嵌紧凑 acceptance summary，保存关键要求、假设、检查方法、状态和证据 path/SHA；无需建立独立需求数据库。
- [x] 画布按 Geometry validity、Engineering checks、Machine judgment 分栏；verified、unverified、not-applicable 分开显示。默认摘要来自当前 workflow obligation，也支持提交时显式记录产品要求。
- [x] 测试固定支架的 `mount-hole-diameter-8mm` 与 `overall-envelope-40x24x12mm`；刻意不给两项证据时均保持 unverified。伪造 verified 且引用不存在证据会被核心拒绝。
- [x] “Open evidence” 通过只读 Agent API 读取当前 run 的受限 evidence JSON；路径校验禁止越界。生产构建、agent contract 生成、全库 TypeScript 296/296 与 `git diff --check` 通过。

**范围限制：** 通用构建工具不负责理解全部自然语言要求。

**开发方式：** 先核对已有实现与未合并修改；沿上述用户行为打通所需状态、真实执行、界面和测试。必要整理先做且保持原行为；不要另拆为纯前端或纯后端票。若一个独立上下文无法完成，按可演示行为继续拆分。

**演示：** 从用户入口执行“交付结果”，展示成功成果、一个相关失败及恢复；以验收中的实际数据核对结果。

**真实操作证据：** `workflow-git.test.ts` 保存孔径、外形尺寸和毫米假设，确认遗漏的尺寸检查没有变成“全部满足”；随后提交带 `status=verified`、`evidenceRef=missing-evidence` 的伪通过摘要，核心以 `verified requirement lacks current-version evidence` 拒绝。摘要随 commit identity 固化，证据同时保留 SHA-256。
