# 21：提交候选并处理机器审查

**状态：** 已完成（2026-03-10）。

**交付结果：** 用户提交当前候选，查看 Reviewer 的发现；失败或中断后可恢复处理。

**前置依赖：** 20 随版本查看工程验收摘要；05 停止任务并继续工作。

**验收条件：**

- [x] Viewer 的 Submit review 入口携带当前候选路径与 SHA，并复用既有 `review-submit` 状态机、权限和 immutable Commit；核心对相同候选/验收契约聚合自然重试，不重复形成独立通过事实。
- [x] Workflow rail 显示 Quick task、Controlled change、Full engineering 及固定 workflow/hash。任务升级进入新 workflow snapshot 并重新受 obligation/permission engine 约束，无法静默降级。
- [x] UI 只有发生在最新成功 build 之后的成功 review 才显示通过；候选变化立即清除通过外观。核心对中断、Reviewer crash、矛盾 preflight、UNRESOLVED、来源或验收契约变化和作者越权均 fail closed。
- [x] 桌面生产构建、activity 13/13、全库 TypeScript 296/296 通过。

**范围限制：** 机器审查不等于人工批准。

**开发方式：** 先核对已有实现与未合并修改；沿上述用户行为打通所需状态、真实执行、界面和测试。必要整理先做且保持原行为；不要另拆为纯前端或纯后端票。若一个独立上下文无法完成，按可演示行为继续拆分。

**演示：** 从用户入口执行“交付结果”，展示成功成果、一个相关失败及恢复；以验收中的实际数据核对结果。

**真实操作证据：** 回归覆盖 `cad_submit_for_review PASS is the only enabled final closure path`、同来源自然重试聚合、source/acceptance contract 改变清票、UNRESOLVED 不投方向票、preflight 矛盾返回可编辑阶段、Reviewer crash 持久化为 UNRESOLVED 并恢复。桌面 activity 测试同时确认停止中的任务不会显示成功。
