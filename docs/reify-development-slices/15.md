# 15：后处理并识别过期仿真

**状态：** 已完成（2026-03-10）。

**交付结果：** 用户对结果做切片、探查和时间步查看；设计变化后知道哪些结果需要重算。

**前置依赖：** 14 按需安装并完成一次仿真。

**验收条件：**

- [x] 真实 VTK 视图提供按 X/Y/Z 平面切片、点索引探针和 2x PNG 截图；当前首个格式是静态 VTU，界面明确说明没有时间步。
- [x] 场、色标范围、表达方式、变形、切片和探针均只改变 VTK 显示管线；界面把材料、载荷、约束、网格明确列为必须重算的分析输入。
- [x] 仿真读取 `model_source` 的路径和 SHA-256，与当前权威 CAD 精确比对。缺失来源、路径变化或哈希变化都会显示“Recompute required”，旧结果仍可查看，并可带旧结果与当前模型身份发起重算。
- [x] 桌面构建通过；仿真元数据与版本失效测试 4/4 通过；全库 TypeScript 回归 295/295 通过；Python 后处理服务通过语法检查。

**范围限制：** 不在显示面板暗中修改工程条件。

**开发方式：** 先核对已有实现与未合并修改；沿上述用户行为打通所需状态、真实执行、界面和测试。必要整理先做且保持原行为；不要另拆为纯前端或纯后端票。若一个独立上下文无法完成，按可演示行为继续拆分。

**演示：** 从用户入口执行“交付结果”，展示成功成果、一个相关失败及恢复；以验收中的实际数据核对结果。

**真实操作证据：** `python3 -m py_compile scripts/desktop-paraview-server.py` 与 `npm --prefix apps/desktop run build` 通过。`simulation-metadata.test.ts` 用 4 点/1 单元的真实 ASCII VTU 核对 10–82 MPa、0–0.34 mm 与 CAD 来源，并验证 STEP 输入被清楚拒绝；`simulation-provenance.test.ts` 验证同路径同哈希保持有效，哈希变化、路径变化和来源缺失均判为需重算。全量 `npm run test:ts` 为 295/295。
